@using F0.Minesweeper.Components.Abstractions.Enums

<div>
	<select name="difficulty" @bind="Selected">
		@foreach (var value in Enum.GetNames<DifficultyLevel>())
		{
			<option id="@value">@value</option>
		}
	</select>
</div>
